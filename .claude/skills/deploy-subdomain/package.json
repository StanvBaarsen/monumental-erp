{
  "name": "deploy-subdomain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deploy a project to a dynamic subdomain under *.monumental.stanvanbaarsen.nl (GitHub + Netlify + Cloudflare).",
  "scripts": {
    "deploy": "tsx index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "execa": "^9.5.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
